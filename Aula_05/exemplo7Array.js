/// EXEMPLO 7 MAP(map)

const numeros = [1,2,3,4]
const quadrados = numeros.map(num => num*num) /// MAP(map) é uma função que aploca na lista para elevar os numeros ao quadrodos(variavel criado para esse exemplo)
console.log(quadrados)