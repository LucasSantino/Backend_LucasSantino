const valor = 50.5; // constantes
const num = 100; //constantes
const a = true; // constantes
console.log(valor, `Tipo da constante ${typeof valor}`);
console.log(num, `${typeof num}`);
console.log(a, `${typeof a}`);
